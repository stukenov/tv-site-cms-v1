<div class="tv-schedule">
  {% for item in items %}
    <div class="program">
      <div class="time">{{ item.time }}</div>
      <div class="program-link">{{ item.program|raw }}</div>
      <div class="description">{{ item.description }}</div>
    </div>
  {% endfor %}
</div>
